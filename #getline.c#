#include "shell.h"


/**
 *custom_getline - funnction that reads the input from user
 */

ssize_t custom_getline(char** ptr, size_t n, FILE *stream)
{
  if (ptr == NULL||n == NULL||stream == NULL)
    {
      perror("Invalid parameters");
      return (-1);
    }

  

  
}

